const DIST_DIR_NAME = 'lib';
const TSCONFIG_PATH = './tsconfig.build.json';

const PACKAGES_NAMES = [
  '@astral/mobx-query',
];

module.exports = {
  DIST_DIR_NAME,
  TSCONFIG_PATH,
  PACKAGES_NAMES,
};
